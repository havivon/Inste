__pycache__/
*.py[cod]
.venv/
venv/
env/

# Local state: the SQLite database, cached covers and - most importantly -
# the Instagram session file. Never commit any of it.
# Anchored: an unanchored "data/" would also swallow the app's data packages.
/data/
*.db
*.db-wal
*.db-shm
ig_session.json

# Android build output
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.apk

.DS_Store
.idea/
.vscode/
